{% include 'default/_form.html.twig' with {
    page_title: 'title.user_edit'|trans({'%id%': users.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: is_granted('ROLE_ADMIN') ? 'user_index' : 'note_index',
} %}
