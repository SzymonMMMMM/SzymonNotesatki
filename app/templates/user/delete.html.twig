{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.user_delete'|trans({'%id%': users.id|default('')}) }}
{% endblock %}

{% block body %}
    {% include 'default/_form.html.twig' with {
        page_title: 'title.user_delete'|trans({'%id%': users.id|default('')}),
        form: form,
        submit_label: 'action.delete'|trans,
        back_to_list_path: 'user_index',
    } %}
{% endblock %}